# NixOS-Configuration

Ignore this repository. I'm new to Nix/NixOS and am now trying to learn it.

## Setup

```console
sudo mv /etc/nixos /etc/nixos.backup
sudo ln -s <path_to_parent_folder> /etc/nixos
sudo nixos-rebuild switch
```
